/** Array of digit banks for joltage calculation */
export type BankArray = string[];

/** A single digit bank string */
export type Bank = string;

/** Joltage value (BigInt for large numbers) */
export type Joltage = bigint;

/** Number of digits to select from a bank */
export type DigitCount = number;
